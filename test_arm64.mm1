import "compiler.mm1";

do {
  -- Set target to ARM64 macOS
  (mmc-set-target "arm64-macos")
  
  (mmc-add '(
    (proc (main)
      -- Simple exit with code 42
      {(exit 42)}
      )
  ))
  
  -- Try to generate the executable
  (print (mmc->string))
};